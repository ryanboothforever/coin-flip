document.addEventListener('DOMContentLoaded', () => {
    console.log('LOADED')
})

function someFunction() {
    console.log('called me!');
    /**
     * TODO: Make fetch GET request to route for coinflip
     * handle result (should be a string that can be 
     * JSON.parse'd), and update the dom appropriately
     * to show/hide the correct quarter
     */
}